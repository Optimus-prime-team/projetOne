### README

```bash
pip install -r requirements.txt
```

```bash
python src/linkedinscrap/link.py
python src/indeed/indeed.py
```

```bash

                            Mode d'emploi :

YOUR_PATH/projetOne [-a="yes/no"] [-s="yes/no"] [-j="job name"] [-c="city name"]

Arguments :

  -a  / --all              : get all in config.json (string)
  -s  / --specific         : specific if you wan't only one (string)
  -j / --job_name          : job name you want (string)
  -c / --city_name         : city name you want (string)

```
